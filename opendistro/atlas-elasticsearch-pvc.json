{
  "apiVersion": "v1",
  "kind": "PersistentVolumeClaim",
  "metadata": {
    "name": "elasticsearch-data",
    "namespace": "default",
    "labels": {
      "app": "elasticsearch"
    }
  },
  "spec": {
    "accessModes": ["ReadWriteMany"],
    "resources": {
      "requests": {
        "storage": "20Gi"
      }
    }
  }
}
